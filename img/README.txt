Coloca en esta carpeta las imágenes utilizadas como fondo de pantalla:

- `baby1.jpg`
- `baby.jpg`

Asegúrate de mantener estos nombres exactamente. El código referencia estas imágenes usando una ruta relativa (`../img/...`) desde `css/`, por eso deben vivir en este mismo directorio `img/`.

Ambos nombres deben coincidir exactamente para que la app muestre las fotos al inicio.
